// The `components` folder contains reusable, feature-agnostic UI
// components (buttons, inputs, layout primitives, etc.) that are not
// tied to any specific feature.
//
// TODO: Add shared UI components as the design system is built out.
