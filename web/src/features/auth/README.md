// The `auth` feature owns authentication UI and logic: login/logout,
// session management, and the AuthProvider context.
//
// TODO: Add registration, password reset, and session persistence logic.
