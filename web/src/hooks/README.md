// The `hooks` folder contains reusable React hooks that are shared
// across multiple features (as opposed to feature-specific hooks,
// which should live inside their respective `features/<name>` folder).
//
// TODO: Add shared hooks (e.g., useMediaQuery, useDebounce) as needed.
