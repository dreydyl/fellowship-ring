// The `services` folder contains the API/service layer: functions
// that wrap Supabase (or other backend) calls, used by features via
// React Query hooks. Business logic belongs in features, not here.
//
// TODO: Add service modules per resource (e.g., devices, sessions).
