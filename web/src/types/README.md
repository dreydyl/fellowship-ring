// The `types` folder contains shared TypeScript types/interfaces used
// across multiple features (e.g., domain models, API DTOs).
//
// TODO: Add shared type definitions as the domain model is defined.
